CREATE OR REPLACE FUNCTION pg_shacrypt(text, text)
RETURNS text
AS 'MODULE_PATHNAME', 'pg_shacrypt'
LANGUAGE C VOLATILE STRICT PARALLEL RESTRICTED ;

CREATE OR REPLACE FUNCTION pg_sha_gensalt(text, int)
    RETURNS text
AS 'MODULE_PATHNAME', 'pg_sha_gensalt'
    LANGUAGE C VOLATILE STRICT PARALLEL RESTRICTED ;

CREATE OR REPLACE FUNCTION pg_sha_gensalt(text)
    RETURNS text
AS 'MODULE_PATHNAME', 'pg_sha_gensalt'
    LANGUAGE C VOLATILE STRICT PARALLEL RESTRICTED ;

